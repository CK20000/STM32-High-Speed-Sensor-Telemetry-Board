The Python telemetry dashboard. Run `main.py` to launch the GUI.
*   `core/`: Handles serial communications (`comms.py`) and gauge logic (`gauges.py`).
*   `ui/`: Contains tab layouts, the PDF viewer, and the `style.qss` stylesheet.
